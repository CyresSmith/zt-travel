import { Suspense } from 'react';

type Props = {};

const NewsPage = (props: Props) => {
    return (
        <Suspense>
            <div>NewsPage</div>;
        </Suspense>
    );
};

export default NewsPage;
